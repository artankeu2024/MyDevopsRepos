# variable 
# scope 
# A variable can be local or global
# global : can seing anuwhere in the program
# local : can only being seen inside the code where is it in

function create local scope

loops and if statement create global variable 
you can use the global keyword to override the value of a local variable with the value of a global variable